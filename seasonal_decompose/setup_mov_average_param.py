
# init moving average params by default or saved from db params - don't change params here!!!

stmss_params.setdefault('period', 12)
stmss_params.setdefault('filt', str4def)
stmss_params.setdefault('two_sided', str4def)
stmss_params.setdefault('model', str4def)
stmss_params.setdefault('extrapolate_trend', str4def)
print()
